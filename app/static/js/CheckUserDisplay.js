const appearanceSelect = document.getElementById('display-select');
// お前はもう、宣言されている
// const userInfoTrs = document.getElementsByClassName('user-info-row');

const display = document.getElementsByClassName('shadow-item');

const showRowByCheckUsers = () => {
  for (let i = 0; i < userInfoTrs.length; i++) {

  }
}
